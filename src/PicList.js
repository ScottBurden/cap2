// import React from 'react';
// import { useSelector } from 'react-redux';

// import Pic from './Pic'

// function PicList() {
//   const items = useSelector(st => Object.values(st.pic).map(
//     p => ({...p, name: `./H&S-${p.id}`})
//   ));
//   return <Pic title="Pic" items={items} />;
// }

// export default PicList;
